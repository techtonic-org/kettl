{
  "name": "kettl",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "cli": "bun run src/cli.ts",
    "test": "for f in src/**/*.test.ts src/*.test.ts; do bun test \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "garmin-connect": "^1.6.2",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
